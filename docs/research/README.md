# research notes

Distilled external documentation the roadmap depends on. Each note carries its
source URLs and a fetched-on date — the x402 ecosystem moves fast; re-verify
anything older than a month before acting on it.

- `bazaar-listing.md` — how CDP Bazaar cataloging actually works (settlement-
  triggered, discovery extension, EXTENSION-RESPONSES pitfalls)
- `mcp-registry-publishing.md` — official MCP registry publish flow

## Pending: Readwise distillation

The operator has saved x402/MCP docs in Readwise. Pulling them requires one
approved `reader_search_documents` MCP call in a Claude session (approval was
unavailable when this note was written). When approved: search for
x402 / Bazaar / MCP / agent-payments saves, distill anything that changes the
roadmap into a note here, and cross-check `docs/ROADMAP.md` against it.
